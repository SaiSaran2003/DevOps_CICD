name: Deploy Robot
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run status check
        run: |
          echo "Robot Assistant active!"
          ls -la
